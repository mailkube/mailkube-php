<?xml version="1.0"?>
<ruleset name="mailkube-docs">
    <description>Documentation gate (#4): public API must be documented and fully type-hinted.</description>

    <file>src</file>
    <!-- Examples are copied by customers, so they are held to the same documentation and
         type-hint rules as src/. -->
    <file>examples</file>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>

    <arg name="colors"/>
    <arg value="p"/>

    <!--
      Require a docblock on every class and function (the "documented" gate). Native PHP types are the
      source of truth, so @param/@return tags are NOT required — only a description. This matches modern
      PHP style (slevomat flags docblocks that merely repeat native types).
    -->
    <rule ref="Squiz.Commenting.ClassComment"/>
    <rule ref="Squiz.Commenting.FunctionComment">
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamTag"/>
        <exclude name="Squiz.Commenting.FunctionComment.MissingParamComment"/>
        <exclude name="Squiz.Commenting.FunctionComment.MissingReturn"/>
    </rule>

    <!-- Complete native type hints (supports the docs + strict-typing story). -->
    <rule ref="SlevomatCodingStandard.TypeHints.ParameterTypeHint"/>
    <rule ref="SlevomatCodingStandard.TypeHints.ReturnTypeHint"/>
    <rule ref="SlevomatCodingStandard.TypeHints.PropertyTypeHint"/>
</ruleset>
